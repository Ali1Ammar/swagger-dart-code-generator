name: example
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ^3.2.3

dependencies:
  chopper:
  collection: 
  http:
  json_annotation: 

dev_dependencies:
  swagger_dart_code_generator:
    path: ../
  chopper_generator:
  build_runner:
  json_serializable:
  lints:
